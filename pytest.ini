[pytest]
testpaths = tests
markers =
    llm: calls the real Ollama server (slow; skip with -m "not llm" if Ollama isn't running)
    i18n: calls the real NLLB translation model (very slow, ~15-50s per call)
